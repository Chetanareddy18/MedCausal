# Streamlit dashboard runtime (Hugging Face Spaces / Streamlit Cloud).
# Reads cached parquet / pickle / npy under outputs/_cache and outputs/tables —
# no model fitting at boot. The full pipeline deps are in requirements-pipeline.txt.
streamlit>=1.37.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
plotly>=5.18.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
